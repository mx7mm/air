# Air inittab
# Minimal init configuration

# Run init script at boot
::sysinit:/etc/init.d/rcS

# Reboot on Ctrl-Alt-Del
::ctrlaltdel:/sbin/reboot

# Shutdown properly - unmount all including /data
::shutdown:/bin/sync
::shutdown:/bin/umount -a -r
